# Copy to .dev.vars for local wrangler dev
# DO NOT commit .dev.vars
FIREBASE_PROJECT_ID=your-firebase-project-id
# Prefer base64 of the service account JSON (avoids dotenv escaping issues):
#   node -e "process.stdout.write(Buffer.from(require('fs').readFileSync('sa.json')).toString('base64'))"
FIREBASE_SERVICE_ACCOUNT_JSON=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Ii4uLn0=
